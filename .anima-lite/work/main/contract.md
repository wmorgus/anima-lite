# Contract: weekly-report-demo
Branch: main
Generated: 2026-07-01
Spine commit: 9c02f5e8
Status: FROZEN FOR SESSION — do not modify without re-running ari-argue

## The argument

Tutors receive a structured, progressive view of their weekly performance: a visually prominent impact section anchors what matters most, time allocation context shows session time against a benchmark, and AI-generated growth insights (disclosed as AI-generated) unlock sequentially — one card focused at a time, the tutor's own review action advancing them. Training recommendations are deferred.

**In scope**: Impact section (with hero card), Time Allocation section, Growth Insights section (expand/collapse invariant, AI disclaimer).
**Deferred**: Recommended Training section — separate port.

## Substrate changes (free to translate)

- React JSX → JSP + ES module JS — rendering medium, argument survives
- Framer Motion + tweenNumber countup → CSS or vanilla JS — animation library is substrate; entrance animations are delight, not an access gate. Respect `prefers-reduced-motion`.
- PLUS DS React components → PlusInterface, plus-badge, Bootstrap equivalents
- SCSS token conventions → prod CSS conventions (body2-txt, gap-between-components-1)
- Mock data → TutorReviewDto / TutorReviewImpactDto / TutorReviewAiInsightDto
- `REPORT_DATA.userName` → `impact.advisorName`; date range → `periodStart`/`periodEnd`
- Stats icon variants (fa-video → fa-calendar-check, color class names) — same stats, substrate label/icon
- Demo CTA section ("Sign up for more sessions") → discard (prototype scaffolding)
- `needsWork` flag in mock data → unused in canonical render, discard
- Session reference text label — both proto and prod are text-only; no clickable recording link
- Feedback button visual states (selected = tonal) → already in prod ai_insights_section.js
- `keyInsight` alert → `dynamicInsight` from TutorReviewImpactDto — same purpose

**Canonical source**: `WeeklyReportContent.jsx`, not `WeeklyReportPage.jsx` (stale prior iteration).

## Claim changes (confirmed with user)

- **Time allocation section** — proto has it; prod explicitly disabled (V1). Decision: **port it** — Confirmed: yes (2026-07-01)
  - Un-comment `TutorReviewTimeAllocationDto` in `TutorReviewDto.java`
  - Populate in `TutorReviewDtoHelper` (un-comment existing commented implementation)
  - Un-comment JSP section in `tutor_review.jsp`
  - Activate `time_allocation_section.js` (exists, dormant)
  - Add benchmark note ("Benchmark: 40% Active Tutoring") to section header
  - Fix exception catch in `getTimeAllocationDto` to set safe defaults (not null) on error

- **Expand/collapse interaction model** — proto: one card expanded at a time. Invariant: only one card is open at any moment. Under-review card auto-expands on render. Reviewed cards toggle. Opening one card closes any other open card (sibling collapse). Locked cards unchanged. Decision: **port it** — Confirmed: yes (2026-07-01)
  - Add module-level `expandedInsightId` state to `ai_insights_section.js`
  - Auto-set to UNDER_REVIEW insight's id on render
  - Wrap card body in `.insight-card-body` div; hide with `d-none` when not expanded
  - Reviewed card headers get click handler: toggle own body + collapse siblings + update `expandedInsightId`
  - UNDER_REVIEW card: expanded by default, not user-togglable (no click handler needed)

- **AI attribution register** — Decision: **keep prod disclaimer** ("generated by AI, which may make mistakes!") — Confirmed: yes (2026-07-01). Prod disclaimer is the appropriate production framing. Do not change.

- **Impact hero card** — proto has a prominent hero block (bolt icon, "Monthly Impact on Students" heading, primary metrics `totalStudentLearningTime` + `conceptsMastered`, trophy insight callout) above the stat tile grid. Prod has flat grid only. Decision: **port it** — Confirmed: yes (2026-07-01)
  - Add hero card container to `impact_section.js` wrapping the two placeholder metrics above stat tiles
  - `totalStudentLearningTime` and `conceptsMastered` render as "--" (placeholders, separate data work)
  - `dynamicInsight` moves into hero card with `fa-trophy` icon (was `fa-lightbulb` in flat layout)
  - Stat tiles remain below hero card

## Notes for ari-port

- `applicationContext.xml` has four commented blocks for time allocation (HBM mapping, service bean, DAO factory entry, DAO bean) — all must be uncommented alongside the DTO/DtoHelper changes
- `time_allocation_section.js` already exists in prod, dormant — activate and wire, don't rewrite
- Sibling-collapse in expand/collapse: when opening a reviewed card, query DOM for any other `.insight-card-body:not(.d-none)` and close it before opening the new one

## Open questions

None. Contract is frozen and ready for ari-port.
