## Blip: four-spine backfill omission (required, per contract)
Severity: info
Location: `.anima-lite/spine-{lumilo-bridge,prod,proto,realtime-event-provider}/` — the four existing spine directories other than `spine-anima-lite` itself
What happened: Claim 3's retroactive-backfill obligation is confirmed (every existing `spine-<label>/` gets a `scaffold.md`, not just probes run from this point forward). This build backfills only `spine-anima-lite`'s own self-spine (the dogfood target named explicitly in plan.md step 9); the other four spine directories are deliberately not touched in this pass.
Why: plan.md step 9's own reasoning, restated — backfilling four more spines is real probe work (each needs its own scenario-naming pass against that repo's own formal.md/telos.md) belonging to those repos' next `ari-map` refresh, not manufactured here against possibly-stale context this workstream has no fresh probe basis for. No spine section requires immediate backfill of every directory in one pass; the obligation is retroactive but not "all at once."
Downstream consequence: the next `/ari-map` refresh of any of those four repos must include a `scaffold.md` backfill as part of that refresh's own commit, named explicitly as a one-time backfill pass (per the new `ari-map/SKILL.md` retroactive-backfill note) rather than silently absorbed into ordinary probe cadence.
Contracting failure?: n/a — the contract explicitly confirmed retroactivity and the plan explicitly scoped this pass's backfill to `spine-anima-lite` only; this is a conservative default operating exactly as designed, not a gap.

## Blip: four-cause-disclaimer site count — ari-map/SKILL.md commit mislabeled as a separate site
Severity: info
Location: `.claude/skills/ari-map/SKILL.md` (Output section, "Scaffold" subsection) and `.claude/skills/ari-map/scaffold-spec.md` — both carry disclaimer text
What happened: the contract's Claim 7 names four canonical sites, one of which is written as "the new `HARNESS.md` §2 spec-ownership row, and `ari-map/SKILL.md`'s Output section / the new `scaffold-spec.md` itself" — i.e. `ari-map/SKILL.md`'s Output section and `scaffold-spec.md` are the *same* site (two document locations naming one artifact's spec home), not two separate sites. This build's commit for `ari-map/SKILL.md` (hash `0506188`) labeled itself "site 2 of 4" and the `scaffold-spec.md` commit (hash `30f90f9`) "site 1 of 4," implying five total counted sites across the four commits rather than four. The actual four confirmed sites are: (1) `ari-map/scaffold-spec.md` + `ari-map/SKILL.md` Output section (one site, two files), (2) `PHILOSOPHY.md`, (3) `spine-anima-lite/telos.md` §3, (4) `HARNESS.md` §2 row. No fifth site was invented or required — the disclaimer text itself is correctly scoped to exactly these locations, only the commit-message numbering over-counted.
Why: contract §"Claim 7 — Detail" wording, cited above, quoted verbatim; the task brief's paraphrase ("ari-map/scaffold-spec.md (see above), PHILOSOPHY.md..., telos.md §3..., and the new HARNESS.md §2 row itself") also lists exactly four, consistent with the contract.
Downstream consequence: none — the actual disclaimer placements are correct and complete (four sites, no more, no fewer). This blip exists only to correct the commit-message trail so a future reader diffing commits against "four sites" doesn't get confused by the "site 1"/"site 2"/"site 3"/"site 4" labels appearing to total four sites across only four of five relevant commits when ari-map/SKILL.md and scaffold-spec.md are actually one site between them.
Contracting failure?: n/a — self-caught mid-build, not a contract gap; the contract's own wording was unambiguous, the miscount was this execution pass's own bookkeeping error, corrected here rather than left silent.

## Blip: material.md drift found — spine described as four files, now five
Severity: info
Location: `.anima-lite/spine-anima-lite/material.md` §4 (Artifact data structures) and §7 (artifact/spec inventory table)
What happened: while writing this repo's own dogfood `scaffold.md` backfill (plan.md step 9), found that `material.md` still stated `spine-<label>/{telos,material,formal,efficient}.md` as "four files per repo" with no mention of the fifth file this workstream's own Claims 1/3 add. This is drift introduced by this workstream's own earlier commits in this same session (not pre-existing drift) — the fact became stale the moment Claim 1's commit landed, and material.md wasn't updated until this later blip surfaced it. Fixed inline (commit `477293c`): added scaffold.md to both the artifact-data-structures list and the §7 inventory table.
Why: `HARNESS.md`'s bidirectional-audit rule ("any skill or agent that reads a doc in the course of work and finds it disagrees with observed disk state" — applied here to this workstream's own artifacts, not a separate doc-drift class) plus `spine-anima-lite/formal.md` §2's own governing rule against a spec's shape going undocumented at its owning location.
Downstream consequence: none remaining — fixed in the same session. Flagged here so the fix is traceable to this workstream rather than looking like an unrelated drive-by edit.
Contracting failure?: n/a — the contract didn't need to anticipate this because it's a same-session consequence of the contract's own confirmed claims, not a pre-existing gap; conservative default was to fix rather than leave a freshly-self-introduced inconsistency standing.

## Blip: dogfood scaffold.md scenario list — judgment call on scope
Severity: info
Location: `.anima-lite/spine-anima-lite/scaffold.md`
What happened: the contract/plan name the dogfood scenarios only by example ("the port pipeline, harness-change pipeline, arete pipeline, ripple, debt-work — whatever `spine-anima-lite/formal.md` already names"). This build named six scenarios (port, harness-change, arete found→judge, ripple, debt-work, read-register) rather than exactly the five examples listed, adding "read register" because `formal.md` §1/§3 documents it as a distinct doorway/flow on equal footing with the others, and material.md §9's work-type vocabulary treats it as a first-class register even though it isn't tagged a `Work-type:` value. Conservative call: included it rather than omitted it, since formal.md clearly documents it as a named flow and omitting a documented flow would make the stub:1 backfill less honest about what "every scenario this repo's spine already documents" actually covers.
Why: `spine-anima-lite/formal.md` §1 ("a pipeline of registers, entered at one of two doorways (change vs. question)... plus a third doorway pair (found vs. judge) for the arete work-type") and §3's `ari-read` stratum, both cited directly as the source for including "read register" as a sixth scenario.
Downstream consequence: a future `/ari-map` refresh of this repo that reaches stub:2/3 on this scaffold should treat this scenario list as a starting point, not a closed set — if that refresh's own probe finds a seventh distinct flow (e.g. `/ari-backlog`'s two-speed capture/sweep, which this build did NOT include as its own scenario since it's explicitly "orthogonal to the port pipeline" per formal.md §3, not a change/question flow in its own right), it should be added then.
Contracting failure?: the contract could have named the exact scenario list rather than "whatever formal.md already names" — leaving it open put a real judgment call (is read-register a "scenario" for this artifact's purposes, is backlog) on this execution pass rather than settling it upstream. Not a severe gap since the contract's own language anticipated deriving the list from formal.md rather than hardcoding it, but worth naming for future dogfood-backfill contracts of this shape.

## Blip: doc-drift missed by execution, caught by independent validation
Severity: info
Location: `CLAUDE.md:33`, `README.md:46`, `README.md:190`
What happened: these three sites still described the spine as "four-cause spine (material/formal/efficient/final)" or `{telos,material,formal,efficient}.md` after Claim 1's commit added a fifth file — the same class of drift blip #3 (`material.md`) self-caught, but not extended to these files during execution. Validation's step C (bidirectional audit) caught it; fixed inline post-validation: all three now name `scaffold.md` alongside the four causes, each with a short "not a fifth cause" pointer rather than a full disclaimer (these are not among the contract's four canonical disclaimer sites — a light mention is proportionate, not a Claim 7 violation).
Why: `HARNESS.md`'s bidirectional-audit rule, same citation as blip #3 — this is the same finding, just not fully propagated the first time.
Downstream consequence: none remaining, fixed same session. Worth noting for future harness-change contracts that touch the spine's own file count: search `CLAUDE.md`/`README.md` explicitly for "four-cause"/"four file" language, not just the spine directory's own files, since the drift surfaces in top-level docs describing the spine as often as in the spine itself.
Contracting failure?: n/a — execution-pass thoroughness gap, not a contract gap; the contract's Claim 7 scoping was correct, this is just an incomplete grep during execution that validation's independent read caught.
