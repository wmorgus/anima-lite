# Feature: Duplicate Seat Map
slug: lumilo-classes-duplicate-seat-map
repo(s): lumilo-bridge
stub: 1
source: ari-map-probe
prod-commit: e2b4831
goes-stale: `ClassConfig` type changes, or duplicate-seat-map.component.ts renamed/removed

## Identity
Clone an existing seat map into a new config. Domain-central (built on `ClassConfig`).

## Entry points
`bridge-frontend/src/app/features/classes/duplicate-seat-map.component.ts`.

## Primary data structure
not traced beyond "uses the same `ClassConfig` types as Class Config Detail."

---
Everything below is enriched by ari-code-rhetoric after a port run.
At stub:0–2, all fields below are `not traced`.

## Full data flow
not traced.

## Client-side wiring
not traced.

## State machine
not traced.

## Feature gates
not traced.

## Seam-specific protocols
not traced.

## Known quirks
not traced.

## Port provenance
n/a until first port.
