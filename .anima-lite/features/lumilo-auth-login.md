# Feature: Login
slug: lumilo-auth-login
repo(s): lumilo-bridge
stub: 0
source: ari-map-probe
prod-commit: e2b4831
goes-stale: bridge-frontend/src/app/features/auth/login.component.ts renamed/removed, or auth provider set changes (graphql-server/src/services/auth/providers/*)

## Identity
User authentication entry point (Google OAuth / JWT per the multi-provider auth backend). not traced beyond existence.

## Entry points
`bridge-frontend/src/app/features/auth/login.component.ts`. not traced further (no GraphQL query/mutation confirmed).

## Primary data structure
not traced.

---
Everything below is enriched by ari-code-rhetoric after a port run.
At stub:0–2, all fields below are `not traced`.

## Full data flow
not traced.

## Client-side wiring
not traced.

## State machine
not traced.

## Feature gates
not traced.

## Seam-specific protocols
not traced.

## Known quirks
not traced.

## Port provenance
n/a until first port.
