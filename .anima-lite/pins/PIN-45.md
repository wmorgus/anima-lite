### PIN-45 — arete's movement-map artifact (ruling 12): shape, home, relationship to the cut
captured: 2026-07-14
stub: 0
status: open
home: anima-lite
goes-stale: not traced
relates-to: reorient/arete.md ruling 12

A `lite`-agent study of `anima-core/plus-uno`'s `uno-blueprint` (Supabase-backed service blueprint — every process fact addressable at `phase → service_scenario → path → step → layer → cell`, single-source/queried-live, tool-coupled read layer, paired-write anti-drift with its PRD, vocabulary-fenced) surfaced a gap in arete's founding pass: it currently produces only the telos statement (final cause), with no first-class artifact for the *formal* cause — how the repo's process actually moves. Ruling 12 (`reorient/arete.md`) folds the direction in but explicitly defers the design: what the codebase analogue of `layer`/`step`/`cell` is (module? execution boundary? call path?), whether this is `ari-arete`'s own output alongside `arete-statement.md` or a separate artifact/skill, whether `ari-arete-pan`'s cut judges against the map as well as the statement, and whether every arete pass needs one or only system-level passes do. Borrow the *shape discipline* (coordinate-addressable, single-source, tool-coupled, anti-drift-paired), not the literal service-blueprint format — that doesn't transpose from "service across humans" to "codebase."

---
Shaping fields — not traced.

**Scope:** not traced
**Batch:** not traced
**Contract:** not traced
**Resolution:** not traced
